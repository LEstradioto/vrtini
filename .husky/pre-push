#!/bin/sh
set -e

npm run lint
npm run format:check
npm run typecheck
# `quality` runs vitest --coverage internally + counts eslint warnings on the
# tracked code-shape rules + checks for circular deps, then gates against
# tools/quality-thresholds.json. Replaces a bare `test:unit` run since the
# quality script wraps it.
npm run quality
